import { AuthStateDirective } from './auth-state.directive';

describe('AuthStateDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthStateDirective();
    expect(directive).toBeTruthy();
  });
});
